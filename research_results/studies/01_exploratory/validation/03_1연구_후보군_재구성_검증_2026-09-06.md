# 1연구 후보군 재구성 검증 — 2026-09-06

상태: **후보군 검증 기록 / 성장설계 부분은 후속 04 문서로 대체됨**

이 문서는 B1/Main/Secondary 후보군을 재구성했을 당시의 검증 기록이다. 여기서 검증한 **후보군과 87 roster 산술은 현재도 유효**하지만, 당시 사용한 `16-point pairwise growth / 16,704 scenarios`는 이후 폐기됐다.

현재 성장설계와 표본 산술은 반드시 다음 문서를 기준으로 한다.

```text
04_성장설계_3단계_완전교차_검증_2026-09-06.md
```

현재 canonical 값:

```text
87 valid rosters
27 growth points
12 environments
324 scenarios / roster
28,188 expected scenarios
```

## 1. 이 문서에서 현재도 유효한 후보군 검증

### B1

```text
liter
anis-star
moran-favorite-item
little-mermaid
rapi-red-hood
```

### Main B3

```text
rapi-red-hood
scarlet-black-shadow
cinderella
cinderella-crystal-wave
liberalio
neon-vision-eye
```

### Secondary B3

```text
epinel
helm
snow-white-heavy-arms
```

roster 산술:

```text
5 B1 × 6 Main × 3 Secondary = 90 raw rosters
Rapi B1 + Rapi Main 중복 3개 제외
= 87 valid rosters
```

## 2. generator 정렬 당시 확인한 항목

`crown_mast_engine/wave_a_study.py`에서 다음을 확인했다.

- study ID `crown-mast-study-01-exploratory-v1`
- B1 후보 5명
- Main 후보 6명
- Secondary 3명
- valid roster 87
- case ID와 label의 canonical study ID 정렬
- `moran-favorite-item`, `scarlet-black-shadow`, `liberalio`를 execution-gated actors로 명시
- `raven`, `quency-escape-queen`, `milk-blooming-bunny`는 현 후보 밖으로 유지

이후 성장 generator는 04 검증에서 3단계 27-point full-cross로 다시 변경됐다.

## 3. 실행 gate

다음 세 캐릭터는 공식 실행 전 model-specific 재검증이 필요하다.

```text
moran-favorite-item
scarlet-black-shadow
liberalio
```

후보군에는 포함하지만 해당 gate가 닫히기 전에는 공식 aggregate를 실행하지 않는다.

## 4. 당시 focused 검증

GitHub Actions run:

```text
33991689279
```

결과:

```text
python -m unittest tests.test_wave_a_study -v
9 tests / 9 passed
```

이 검증은 당시 후보군 exact match, 90 raw / 87 valid roster, 라피 중복 제외, study ID 정렬, execution gate 표기 등을 확인했다.

당시 성장축은 16-point pairwise였으므로 그 부분은 **현재 설계 근거로 사용하지 않는다.**

## 5. 당시 전체 회귀 검증

GitHub Actions run:

```text
33991715546
```

결과:

```text
318 tests
315 passed
3 failed
```

실패 3건:

```text
test_analysis.RotationAnalysisTests.test_default_liter_baseline_has_standard_break_even
test_research.ComparisonReportTests.test_report_preserves_baseline_totals_and_research_metrics
test_scarlet_black_shadow.ScarletBlackShadowAnalysisTests.test_strong_distributed_secondary_still_favors_conventional
```

관측된 차이는 20억대 총딜에서 약 `0.00005~0.00011` 수준의 초미세 numerical mismatch였다. 후보군 또는 roster generator invariant 실패는 아니었다.

따라서 현재도 공식 실행 전에 이 numerical regression 3건을 별도로 분류하고 clean full regression을 확보해야 한다.

## 6. 현재 해석

이 문서에서 현재 설계에 계속 사용하는 것은 다음뿐이다.

```text
B1 5 / Main 6 / Secondary 3
90 raw / 87 valid rosters
Moran/SBS/Liberalio execution gate
당시 full regression numerical mismatch 기록
```

다음 값은 **폐기된 과거 설계값**이다.

```text
16-point pairwise growth
192 scenarios / roster
16,704 total scenarios
```

현재 실행 설계는 04 검증과 canonical `03_1연구_실행_설계_확정본.md`의 `27 growth / 324 per roster / 28,188 total`을 따른다.
