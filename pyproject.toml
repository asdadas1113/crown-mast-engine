[project]
name = "crown-mast-engine"
version = "0.1.0"
description = "Research engine for NIKKE Crown and Mast: Romantic Maid rotations"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.package-data]
crown_mast_engine = ["data/*.json", "web/*.html", "web/*.css", "web/*.js"]

[tool.unittest]
start-directory = "tests"
